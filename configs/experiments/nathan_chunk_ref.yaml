# Only need to specify values that override the default config_models.py
experiment_name: "nathan_chunk_ref"
description: "hehehe"

# --- Data Source ---
csv_file_path: "data/test.csv"
chunk_id: "001"
character: "Nathan"

# --- Generation Logic ---
mode: "chunk"

# --- Refinement ---
refine_prompt: True
refiner_template_name: "sketch"

# Default generation parameters for the image model
parameters:
  initial_num_steps: 50

refiner_parameters:
  temperature: 0.7
  max_tokens: 250