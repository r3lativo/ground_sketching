# Only need to specify values that override the default config_models.py
experiment_name: "nathan_utt_raw"
description: "hehehe"

# --- Data Source ---
csv_file_path: "data/test.csv"
chunk_id: "001"
character: "Nathan"

# --- Generation Logic ---
mode: "utterance"

# Default generation parameters for the image model
parameters:
  initial_num_steps: 50
  # guidance_scale is ignored since the model is not guidance-distilled.
